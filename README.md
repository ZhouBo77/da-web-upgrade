# da-web-upgrade

Fullstack starter scaffold.

## Structure
- `frontend/` React + Vite
- `backend/` Node + Express API

## Quick start
```bash
# backend
cd backend && npm install && npm run dev

# frontend (new terminal)
cd frontend && npm install && npm run dev
```
